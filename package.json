{
  "name": "viral-hook-generator-api",
  "version": "1.0.1",
  "description": "Secure API for the Viral Hook Generator.",
  "main": "api/generate.js",
  "dependencies": {
    "@google/genai": "^1.30.0",
    "firebase-admin": "^11.11.0",
    "stripe": "^14.15.0",
    "resend": "^3.0.0"
  },
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "build": "echo 'Nothing to build'",
    "start": "echo 'This is a serverless function, run in Vercel'",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
